# Feature Development Workflow
# Use this workflow when implementing a new feature.

name: feature-development
description: End-to-end workflow for developing a new feature.

steps:
  - name: analysis
    description: Analyze the feature request, explore the codebase, and identify affected areas.
    recommended_agents:
      - claude
      - gemini
    tasks:
      - Read and understand the feature requirements
      - Explore the codebase to identify relevant files and modules
      - Identify dependencies and potential impact areas
      - Document findings in the task file
    inputs:
      - Feature request or issue description
      - Project codebase
    outputs:
      - Updated task file with analysis section
      - List of affected files
      - Identified risks or constraints

  - name: design
    description: Create a technical design for the feature implementation.
    recommended_agents:
      - claude
      - gemini
    tasks:
      - Define interfaces and data structures
      - Outline the implementation approach
      - Identify edge cases and error handling strategies
      - Document the design in the task file
    inputs:
      - Analysis results from previous step
      - Project architecture and conventions
    outputs:
      - Updated task file with design section
      - Interface definitions
      - Implementation plan

  - name: implementation
    description: Implement the feature according to the design.
    recommended_agents:
      - codex
      - cursor
    tasks:
      - Create a feature branch
      - Implement the feature code
      - Write unit tests
      - Write integration tests (if applicable)
      - Ensure code follows project conventions
    inputs:
      - Technical design from previous step
      - Project coding standards
    artifact_versioning:
      outputs:
        - name: implementation
          pattern: "implementation.md | implementation-r{N}.md"
          rule: "Scan existing implementation artifacts; create implementation.md for Round 1, then implementation-r{N}.md for later re-implementations (N = highest existing round + 1)"
    outputs:
      - Feature branch with implementation
      - Test files
      - Updated task file with implementation notes

  - name: review
    description: Review the implementation for correctness, style, and best practices.
    recommended_agents:
      - claude
    tasks:
      - Review code changes for correctness
      - Check adherence to project conventions
      - Verify test coverage and quality
      - Check for security and performance issues
      - Create a review report
    inputs:
      - Implementation branch
      - Project conventions
    artifact_versioning:
      inputs:
        - name: implementation
          pattern: "implementation.md | implementation-r{N}.md"
          rule: "Read the highest-round implementation artifact as the review input"
      outputs:
        - name: review
          pattern: "review.md | review-r{N}.md"
          rule: "Scan existing review artifacts; create review.md for Round 1, then review-r{N}.md for later rounds (N = highest existing round + 1)"
    outputs:
      - Review report
      - List of issues to fix

  - name: fix
    description: Address issues found during the review phase.
    recommended_agents:
      - codex
      - cursor
    tasks:
      - Fix all critical and major issues from the review
      - Address minor issues and suggestions as appropriate
      - Update tests if needed
      - Verify all tests pass
    inputs:
      - Review report
      - Implementation branch
    artifact_versioning:
      inputs:
        - name: review
          pattern: "review.md | review-r{N}.md"
          rule: "Read the highest-round review artifact and verify it matches the most recent Code Review entry in task.md Activity Log"
      outputs:
        - name: implementation
          pattern: "implementation.md | implementation-r{N}.md"
          rule: "Append the fix log to the highest-round implementation artifact and include the review artifact filename in the heading"
    outputs:
      - Updated branch with fixes
      - Updated task file

  - name: commit
    description: Finalize changes and create a pull request.
    recommended_agents:
      - claude
      - human
    tasks:
      - Ensure all tests pass
      - Write clear commit messages following conventions
      - Create a pull request with proper description
      - Link the PR to related issues
      - Move task to completed
    inputs:
      - Final implementation branch
      - Task file
    outputs:
      - Pull request
      - Completed task file in .agent-workspace/completed/
